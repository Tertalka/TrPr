# 🏗️ Архітектура проекту

## 📐 Структура компонентів

```
App (головний компонент)
├── State Management
│   ├── products - список товарів з API
│   ├── loading - індикатор завантаження
│   ├── error - обробка помилок
│   ├── selectedProduct - товар у модалі
│   ├── modalOpen - стан модального вікна
│   └── favorites - список вибраних товарів
│
├── AppBar (Material-UI)
│   ├── Заголовок програми
│   └── Badge з кількістю вибраних
│
├── Grid контейнер (2 колони)
│   │
│   ├── Ліва колона (8/12 ширини)
│   │   └── ProductsTable
│   │       ├── props: products, onRowClick, favorites
│   │       └── events: row click, favorite toggle
│   │
│   └── Права колона (4/12 ширини - sticky)
│       └── FavoritesPanel
│           ├── props: favorites, onRemove
│           └── events: item remove
│
└── ProductModal (Dialog)
    ├── props: product, open, onClose, onAddToFavorites, isFavorite
    └── events: close, add to favorites
```

## 🔄 Потік даних (Data Flow)

```
1. ІНІЦІАЛІЗАЦІЯ
   ↓
   App.useEffect() запускається
   ↓
   axios.get('https://fakestoreapi.com/products')
   ↓
   Отримано 20+ товарів
   ↓
   setProducts(data) - оновлено state
   ↓
   Таблиця рендериться

2. КОРИСТУВАЧ КЛІКА НА РЯДОК
   ↓
   ProductsTable.handleRowClick(product)
   ↓
   setSelectedProduct(product)
   ↓
   setModalOpen(true)
   ↓
   ProductModal рендериться

3. КОРИСТУВАЧ ДОДАЄ У ВИБРАНЕ
   ↓
   ProductModal.handleAddClick() ЛІ ProductsTable.handleFavoriteClick()
   ↓
   App.handleAddToFavorites(product)
   ↓
   setFavorites([...favorites, product])
   ↓
   FavoritesPanel оновляється
   ↓
   Badge оновляється

4. КОРИСТУВАЧ ВИДАЛЯЄ З ВИБРАНОГО
   ↓
   FavoritesPanel.handleRemove(productId)
   ↓
   App.handleRemoveFromFavorites(productId)
   ↓
   setFavorites(filtered)
   ↓
   UI оновляється
```

## 🔌 API Інтеграція

### FakeStore API
- **URL:** https://fakestoreapi.com/products
- **Метод:** GET
- **Тип:** JSON
- **Кількість товарів:** ~20
- **Кеширування:** Нема (завантажується кожен раз)

### Структура відповіді:
```json
{
  "id": 1,
  "title": "Fjallraven - Foldsack...",
  "price": 109.95,
  "description": "Your perfect pack...",
  "category": "electronics",
  "image": "https://...",
  "rating": {
    "rate": 3.9,
    "count": 120
  }
}
```

## 🎨 Компоненти Material-UI

| Компонент | Використання | Причина |
|-----------|-------------|----------|
| `AppBar` | Верхня панель | Професійний вигляд |
| `Table` | Таблиця товарів | Стандартне відображення даних |
| `Dialog` | Модальне вікно | Модальне вікно з повною інформацією |
| `Button` | Кнопки дій | Стандартні елементи управління |
| `IconButton` | Кнопки з іконками | Компактне управління |
| `Chip` | Категорії | Компактне відображення меток |
| `Grid` | Макет | Адаптивна сітка |
| `Container` | Контейнер контенту | Центрування та padding |
| `Paper` | Контейнер з тінню | Виділення блоків |
| `CircularProgress` | Спінер | Індикатор завантаження |
| `Alert` | Помилки | Повідомлення користувачеві |
| `Rating` | Рейтинг | Звездочки для оцінки |
| `Badge` | Значок | Кількість вибраних товарів |
| `Divider` | Розділювач | Візуальне розділення |
| `Tooltip` | Підказки | Допоміжна інформація |

## 📊 State Management

### App.jsx State:
```javascript
const [products, setProducts] = useState([])          // Список товарів
const [loading, setLoading] = useState(true)          // Індикатор завантаження
const [error, setError] = useState(null)              // Помилка API
const [selectedProduct, setSelectedProduct] = useState(null) // Поточний товар
const [modalOpen, setModalOpen] = useState(false)     // Стан модалі
const [favorites, setFavorites] = useState([])        // Вибрані товари
```

### Event Handlers:
```javascript
handleRowClick(product)        // Відкриває модаль
handleCloseModal()             // Закриває модаль
handleAddToFavorites(product)  // Додає у вибране
handleRemoveFromFavorites(id)  // Видаляє з вибраного
```

## 🎯 Ключові особливості

### 1. **Асинхронна завантаження**
```javascript
useEffect(() => {
  const fetchProducts = async () => {
    try {
      const response = await axios.get('...');
      setProducts(response.data);
    } catch (err) {
      setError(err.message);
    } finally {
      setLoading(false);
    }
  };
  fetchProducts();
}, []);
```

### 2. **Умовне рендерування**
```javascript
{loading ? <CircularProgress /> : <ProductsTable ... />}
{error && <Alert severity="error">{error}</Alert>}
{selectedProduct && <ProductModal ... />}
```

### 3. **Обробка подій з зупинкою поширення**
```javascript
const handleFavoriteClick = (e, product) => {
  e.stopPropagation(); // Запобігає відкриттю модалі
  // ...
};
```

### 4. **Перевірка стану**
```javascript
const isFavorite = (productId) => {
  return favorites.some(fav => fav.id === productId);
};
```

## 🌐 Адаптивність

Використовуються Grid Break points Material-UI:

```javascript
<Grid item xs={12} md={8}>     // 100% на мобілі, 66% на десктопі
<Grid item xs={12} md={4}>     // 100% на мобілі, 33% на десктопі
```

## 📦 Залежності та їх ролі

| Пакет | Версія | Роль |
|-------|--------|------|
| react | ^19.2.0 | Основний фреймворк |
| axios | ^1.7.7 | HTTP запити |
| @mui/material | ^5.x.x | UI компоненти |
| @mui/icons-material | ^5.x.x | Іконки |
| @emotion/react | ^11.x.x | CSS-in-JS для стилів |
| @emotion/styled | ^11.x.x | Styled компоненти |
| prop-types | ^15.x.x | Валідація пропсів |

## 🚀 Оптимізаційні стратегії

1. **Ленива завантаження** - спінер під час завантаження
2. **Обробка помилок** - Alert повідомлення користувачеві
3. **Мемоізація** - функції handleRowClick та інші не пересоздаються
4. **Умовне рендерування** - компоненти рендеряться тільки коли потрібно
5. **Sticky позиціонування** - панель вибраного залишається видимою при прокруці

---

**Надано документом для розуміння архітектури та потоків даних**
