# 📂 Структура проекту

## 🎯 Кореневі файли

| Файл | Опис |
|------|------|
| `package.json` | Конфігурація проекту та залежності |
| `package-lock.json` | Версії залежностей (заблоковані) |
| `index.html` | HTML сторінка (точка входу) |
| `vite.config.js` | Конфігурація Vite сервера |
| `eslint.config.js` | Конфігурація перевірки коду |
| `.gitignore` | Файли для ігнорування Git |

## 📚 Документація (Markdown файли)

| Файл | Опис |
|------|------|
| `QUICKSTART.md` | ⭐ **ПОЧНІТЬ ОТУТ** - швидкий старт (3 кроки) |
| `DOCUMENTATION.md` | Основна документація проекту |
| `TESTING.md` | Інструкції та сценарії тестування |
| `ARCHITECTURE.md` | Архітектура та потік даних |
| `CODE_DOCUMENTATION.md` | Документація коду та прикладів |
| `TECH_SPECS.md` | Технічні характеристики та залежності |
| `SUBMISSION.md` | Звіт про виконання завдання |
| `README.md` | Стандартний файл (основна інформація) |

## 📁 Папка `src/` (Вихідний код)

### Основні файли
```
src/
├── App.jsx                # Головний компонент (управління state)
├── App.css                # Стилі програми
├── main.jsx              # Entry point (запуск програми)
├── index.css             # Глобальні стилі
├── components/           # Папка з компонентами
│   ├── ProductsTable.jsx # Таблиця товарів (Material-UI Table)
│   ├── ProductModal.jsx  # Модальне вікно (Material-UI Dialog)
│   └── FavoritesPanel.jsx # Панель вибраного товарів
└── assets/               #静ичные файлы (лого, зображення)
    └── react.svg        # React логотип
```

### Опис компонентів

#### `App.jsx` (Головний компонент)
```javascript
// Управління stanом
- products: список товарів з API
- loading: індикатор завантаження
- error: помилка з API
- selectedProduct: поточний товар
- modalOpen: видимість модалі
- favorites: вибрані товари

// Функції
- fetchProducts(): axios запит до API
- handleRowClick(): відкриває модаль
- handleAddToFavorites(): додає у вибране
- handleRemoveFromFavorites(): видаляє з вибраного

// Структура
<AppBar> - верхня панель
<Grid>
  <Grid item xs={12} md={8}>
    <ProductsTable />
  </Grid>
  <Grid item xs={12} md={4}>
    <FavoritesPanel />
  </Grid>
</Grid>
<ProductModal /> - модальне вікно
```

#### `ProductsTable.jsx` (Таблиця товарів)
```javascript
// Props
- products: array
- onRowClick: функція при кліку на рядок
- favorites: масив вибраних товарів

// Вивід
Таблиця з колонками:
- Назва продукту
- Категорія (Chip)
- Ціна
- Рейтинг (зірки)
- Вибране (іконка серця)

// События
- Клік на рядок: openModal()
- Клік на серце: toggleFavorite()
```

#### `ProductModal.jsx` (Модальне вікно)
```javascript
// Props
- product: об'єкт товару
- open: видимість
- onClose: закриття
- onAddToFavorites: додавання
- isFavorite: вже обраний?

// Вивід в модалі
- Назва товару
- Категорія
- Опис (повний текст)
- Ціна
- Рейтинг (Rating компонент)
- ID продукту
- Кнопки дій

// События
- Клік на X або "Закрити": closeModal()
- Клік на "Додати": addToFavorites()
```

#### `FavoritesPanel.jsx` (Панель вибраного)
```javascript
// Props
- favorites: масив вибраних товарів
- onRemove: видалення товару

// Вивід
- Список вибраних товарів
- Статистика (кількість, сума)
- Кнопка видалення для кожного товару
- Порожня сторінка при відсутності

// События
- Клік на видалення: removeFromFavorites()
```

## 📦 Папка `public/`

```
public/
└── vite.svg  # Favicon
```

## 🏗️ Папка `dist/` (Production версія)

```
dist/
├── index.html              # Мініфікований HTML
├── assets/
│   ├── index-HASH.css     # Мініфікований CSS
│   └── index-HASH.js      # Мініфікований JavaScript
└── vite.svg               # Статичні файли

Розмір: ~600 KB (розпаковане)
        ~150 KB (gzipped)
```

## 🔧 node_modules/ (Залежності)

```
node_modules/
├── react/
├── react-dom/
├── axios/
├── @mui/material/
├── @mui/icons-material/
├── @emotion/react/
├── @emotion/styled/
├── prop-types/
├── vite/
└── ... (інші 200+ пакетів)

Розмір: ~500 MB
```

## 📊 Обсяг проекту

| Категорія | Файлів | Розмір |
|-----------|--------|--------|
| Вихідний код (src/) | 7 | ~5 KB |
| Документація | 8 | ~50 KB |
| Config файли | 4 | ~2 KB |
| HTML/CSS | 2 | ~3 KB |
| **Всього без node_modules** | **~21** | **~60 KB** |
| node_modules | - | ~500 MB |
| dist/ | - | ~600 KB |

## 🔍 Як знайти конкретне

### Хочу знайти компонент таблиці
→ `src/components/ProductsTable.jsx`

### Хочу змінити стилі
→ `src/App.css` або `src/index.css`

### Хочу дізнатися про API
→ `TECH_SPECS.md` або `CODE_DOCUMENTATION.md`

### Хочу запустити проект
→ `QUICKSTART.md`

### Хочу тестувати функції
→ `TESTING.md`

### Хочу зрозуміти архітектуру
→ `ARCHITECTURE.md`

### Хочу дізнатися про залежності
→ `package.json` або `TECH_SPECS.md`

## 📋 Чеклист для перевірки файлів

Переконайтесь, що присутні:

- [x] `src/App.jsx` - головний компонент
- [x] `src/components/ProductsTable.jsx` - таблиця
- [x] `src/components/ProductModal.jsx` - модаль
- [x] `src/components/FavoritesPanel.jsx` - вибране
- [x] `package.json` - залежності
- [x] `vite.config.js` - конфігурація
- [x] `index.html` - HTML сторінка
- [x] Всі документація файлі

---

**Останнє оновлення:** 28 Листопада 2025  
**Структура готова до здачі** ✅
